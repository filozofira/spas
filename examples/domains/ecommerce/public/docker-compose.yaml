# Generated by spas-compose
# Domain: public
# Generated: 2025-12-17T21:43:02.527Z

services:
  redis:
    image: redis:7-alpine
    container_name: spas-redis
    ports:
      - "6379:6379"
    networks:
      - spas-network
    healthcheck:
      test:
        - CMD
        - redis-cli
        - ping
      interval: 5s
      timeout: 3s
      retries: 3
  zipkin:
    image: openzipkin/zipkin:latest
    container_name: spas-zipkin
    ports:
      - "9411:9411"
    networks:
      - spas-network
  inventory-service:
    container_name: spas-inventory-service
    ports:
      - "5001:8080"
    environment:
      - SERVICE_NAME=inventory-service
      - SIDECAR_PORT=7000
      - PORT=8080
      - ZIPKIN_URL=http://zipkin:9411
    networks:
      - spas-network
    image: spas-examples/inventory-service@sha256:4f6de5ba301c4da2570d659abca21189245e887abd539756063142007a6ce273
  inventory-service-sidecar:
    image: spas/sidecar:latest
    container_name: inventory-service-sidecar
    environment:
      - SIDECAR_PORT=7000
      - CONFIG_PATH=/app/config.json
      - SERVICE_NAME=inventory-service
      - ZIPKIN_URL=http://zipkin:9411
      - SERVICE_PORT=8080
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    volumes:
      - ./config.inventory-service.json:/app/config.json
      - ./transformations/inventory-service:/app/transformations
    networks:
      - spas-network
    depends_on:
      - redis
      - zipkin
  order-service:
    container_name: spas-order-service
    ports:
      - "5002:8080"
    environment:
      - SERVICE_NAME=order-service
      - SIDECAR_PORT=7000
      - PORT=8080
      - ZIPKIN_URL=http://zipkin:9411
    networks:
      - spas-network
    image: spas-examples/order-service@sha256:bab325ae683beacead69c21a845cac080be7b1f4447dffcd1125eb3ff6096cbb
  order-service-sidecar:
    image: spas/sidecar:latest
    container_name: order-service-sidecar
    environment:
      - SIDECAR_PORT=7000
      - CONFIG_PATH=/app/config.json
      - SERVICE_NAME=order-service
      - ZIPKIN_URL=http://zipkin:9411
      - SERVICE_PORT=8080
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    volumes:
      - ./config.order-service.json:/app/config.json
      - ./transformations/order-service:/app/transformations
    networks:
      - spas-network
    depends_on:
      - redis
      - zipkin
networks:
  spas-network:
    driver: bridge
