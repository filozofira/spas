/* inbound-stock-reserved.jsonata */
/* Transforms stock-reserved (inventory-service) -> confirm-order (order-service) */
{
  "orderId": orderId,
  "reservedItems": $append([], reservations.{
    "productId": productId,
    "quantity": quantity
  })
}
