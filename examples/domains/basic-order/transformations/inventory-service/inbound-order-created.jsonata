/* inbound-order-created.jsonata */
/* Transforms order-service.order-created -> inventory-service.reserve-stock */
{
  "referenceId": referenceId ? referenceId : orderId,
  "items": $append([], items.{
    "itemId": productId,
    "quantity": quantity
  })
}
