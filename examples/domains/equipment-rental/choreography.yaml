# SPAS Choreography Configuration
# See: specs/005-spas-compose-cli/contracts/choreography-schema.yaml

version: "1.0"
domain: equipment-rental

# Named choreography flows
# Use /spas.compose agent to generate flows based on service contracts
flows:
  rental-fulfillment-flow:
    description: "Equipment rental request to inventory reservation and fulfillment"
    participants:
      - rental-service
      - inventory-service
      - fulfillment-service
    commands:
      - service: rental-service
        command: request-rental
        endpoint: /rentals
    events:
      - source: rental-service
        event: rental-requested
        topic: rental-events
        targets:
          - service: inventory-service
            command: reserve-stock
            transform: transformations/inventory-service/inbound-rental-requested.jsonata
          - service: fulfillment-service
            command: create-shipment
            transform: transformations/fulfillment-service/inbound-rental-requested.jsonata
      - source: inventory-service
        event: stock-reserved
        topic: inventory-events
        targets: []
      - source: inventory-service
        event: stock-depleted
        topic: inventory-events
        targets: []
      - source: fulfillment-service
        event: shipment-created
        topic: fulfillment-events
        targets: []

  rental-return-flow:
    description: "Equipment return processing to release inventory"
    participants:
      - rental-service
      - inventory-service
    commands:
      - service: rental-service
        command: return-rental
        endpoint: /rentals/{id}/return
    events:
      - source: rental-service
        event: rental-returned
        topic: rental-events
        targets:
          - service: inventory-service
            command: release-stock
            transform: transformations/inventory-service/inbound-rental-returned.jsonata
      - source: inventory-service
        event: stock-released
        topic: inventory-events
        targets: []

# Infrastructure components (optional)
infrastructure:
  redis:
    enabled: true
    # host: redis
    # port: 6379
  zipkin:
    enabled: true
    # url: http://zipkin:9411
