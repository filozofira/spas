{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://spas.dev/example/schemas/get-payment.schema.json",
  "type": "object",
  "required": ["id", "status"],
  "properties": {
    "id": {"type": "string"},
    "status": {"type": "string", "enum": ["pending", "completed", "failed"]}
  }
}