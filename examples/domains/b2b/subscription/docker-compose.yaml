# Generated by spas-compose
# Domain: subscription
# Generated: 2025-12-17T22:56:23.303Z

services:
  redis:
    image: redis:7-alpine
    container_name: spas-redis
    ports:
      - "6379:6379"
    networks:
      - spas-network
    healthcheck:
      test:
        - CMD
        - redis-cli
        - ping
      interval: 5s
      timeout: 3s
      retries: 3
  zipkin:
    image: openzipkin/zipkin:latest
    container_name: spas-zipkin
    ports:
      - "9411:9411"
    networks:
      - spas-network
  inventory-service:
    container_name: spas-inventory-service
    ports:
      - "5001:8080"
    environment:
      - SERVICE_NAME=inventory-service
      - SIDECAR_PORT=7000
      - PORT=8080
      - ZIPKIN_URL=http://zipkin:9411
    networks:
      - spas-network
    image: spas-examples/inventory-service@sha256:04837fe89384aea85eaeba0fa8ca00de620bf6e93777ae90797558d126d5fff4
  inventory-service-sidecar:
    image: spas/sidecar:latest
    container_name: inventory-service-sidecar
    environment:
      - SIDECAR_PORT=7000
      - CONFIG_PATH=/app/config.json
      - SERVICE_NAME=inventory-service
      - ZIPKIN_URL=http://zipkin:9411
      - SERVICE_PORT=8080
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    volumes:
      - ./config.inventory-service.json:/app/config.json
      - ./transformations/inventory-service:/app/transformations
    networks:
      - spas-network
    depends_on:
      - redis
      - zipkin
  order-service:
    container_name: spas-order-service
    ports:
      - "5002:8080"
    environment:
      - SERVICE_NAME=order-service
      - SIDECAR_PORT=7000
      - PORT=8080
      - ZIPKIN_URL=http://zipkin:9411
    networks:
      - spas-network
    image: spas-examples/order-service@sha256:a4cee00f0b7477f730ea9dad7f1404d2570d4ce99bb92ac853397b6e89e14b08
  order-service-sidecar:
    image: spas/sidecar:latest
    container_name: order-service-sidecar
    environment:
      - SIDECAR_PORT=7000
      - CONFIG_PATH=/app/config.json
      - SERVICE_NAME=order-service
      - ZIPKIN_URL=http://zipkin:9411
      - SERVICE_PORT=8080
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    volumes:
      - ./config.order-service.json:/app/config.json
      - ./transformations/order-service:/app/transformations
    networks:
      - spas-network
    depends_on:
      - redis
      - zipkin
  subscription-service:
    container_name: spas-subscription-service
    ports:
      - "5003:8080"
    environment:
      - SERVICE_NAME=subscription-service
      - SIDECAR_PORT=7000
      - PORT=8080
      - ZIPKIN_URL=http://zipkin:9411
    networks:
      - spas-network
    image: spas-examples/subscription-service@sha256:7bab4e9cf2482f1dea73bb9698f1e96c1fb629ebc6d2ad50caf67a6a07643063
  subscription-service-sidecar:
    image: spas/sidecar:latest
    container_name: subscription-service-sidecar
    environment:
      - SIDECAR_PORT=7000
      - CONFIG_PATH=/app/config.json
      - SERVICE_NAME=subscription-service
      - ZIPKIN_URL=http://zipkin:9411
      - SERVICE_PORT=8080
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    volumes:
      - ./config.subscription-service.json:/app/config.json
      - ./transformations/subscription-service:/app/transformations
    networks:
      - spas-network
    depends_on:
      - redis
      - zipkin
networks:
  spas-network:
    driver: bridge
