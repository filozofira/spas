/* inbound-rental-requested.jsonata */
/* Transforms rental-requested (rental-service) -> reserve-stock request (inventory-service) */
{
  "referenceId": rentalId,
  "items": $append([], items.{
    "itemId": productId,
    "quantity": quantity
  })
}
