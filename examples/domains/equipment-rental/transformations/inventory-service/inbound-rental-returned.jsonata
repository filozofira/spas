/* inbound-rental-returned.jsonata */
/* Transforms rental-returned (rental-service) -> release-stock request (inventory-service) */
{
  "referenceId": rentalId,
  "items": $append([], items.{
    "productId": productId,
    "quantity": quantity
  })
}
