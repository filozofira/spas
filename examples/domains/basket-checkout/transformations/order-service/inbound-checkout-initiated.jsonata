/* inbound-checkout-initiated.jsonata */
/* Transforms checkout-initiated (basket-service) -> create-order (order-service) */
{
  "customerId": customerId,
  "items": $append([], items.{
    "productId": productId,
    "quantity": quantity,
    "price": 0
  }),
  "total": 0,
  "shippingAddress": shippingAddress,
  "referenceId": basketId
}
