/* inbound-order-created.jsonata */
/* Transforms order-created (order-service) → ReserveStockRequest (inventory-service) */
/* Maps order items to inventory reservation request */
{
  "orderId": orderId,
  "items": $append([], items.{
    "productId": productId,
    "quantity": quantity
  })
}
