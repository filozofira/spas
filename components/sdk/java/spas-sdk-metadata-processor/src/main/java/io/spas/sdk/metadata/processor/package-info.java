/**
 * SPAS SDK Metadata Processor module.
 * 
 * <p>Provides compile-time annotation processing to generate {@code spas.json}.</p>
 * 
 * <p>This processor scans for {@code @SpasCommand}, {@code @SpasQuery}, and
 * {@code @SpasEvent} annotations and generates metadata at compile time.</p>
 */
package io.spas.sdk.metadata.processor;
